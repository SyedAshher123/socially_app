<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color (black) -->
    <solid android:color="#FF000000" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Border -->
    <stroke
        android:width="0.5dp"
        android:color="@color/light_brown" /> <!-- faint white border -->
</shape>
